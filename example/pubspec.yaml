name: native_textfield_tv_example
description: "Demonstrates how to use the native_textfield_tv plugin."
publish_to: 'none'

environment:
  sdk: ^3.6.0-332.0.dev
dependencies:
  flutter:
    sdk: flutter
  native_textfield_tv:
    path: ../
  cupertino_icons: ^1.0.8

dev_dependencies:
  integration_test:
    sdk: flutter
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0
flutter:
  uses-material-design: true